Q. Create a Kubernetes Secret named database-app-secret in the default namespace using the contents of the file database-data.txt

Sol. kubectl create secret generic database-app-secret --from-file=database-data.txt --namespace=default
